pipeline {
    agent any //where to execute 

    stages {
        stage('build') {
            steps {
                echo 'BUILD STAGE'
            }

        stage('test') {
            steps {
                echo 'TEST STAGE'
            }

        stage('deploy') {
            steps {
                echo 'DEPLOY STAGE'
            }
        }
    }
}
